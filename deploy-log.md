# P3 部署记录

> 用途：记录服务器上每一条重要命令——干了什么、为什么这么干、出了什么现象。
> 它既是排错手册，也是面试作品《部署记录》的雏形。
> 写法：每跑通一条新命令，就加一条记录。宁短勿断。

## 服务器档案

| 项目 | 内容 |
|------|------|
| 云厂商 | 阿里云 ECS（上海） |
| 配置 | 2核2G（ecs.e-c1m1.large） |
| 系统 | Ubuntu（重置后以 `uname -a` 输出为准） |
| 公网 IP | 8.159.\*\*\*.\*\*\*（公开仓库里打码，不写全） |
| 登录用户 | root |

## 操作日志

### 2026-09-16 · 第一次连上服务器

```bash
ssh root@8.159.129.135
```

- 第一次连：问 `Are you sure...(yes/no)` → 输 `yes`；问 `password` → 盲打（屏幕不显示是正常的）
- 退出服务器：`exit`
- 今天为了连上它折腾了一路，关键排错经验单独记在《问题排查指南》里，这里只记结论
## 2026-09-18 本地 Dify 部署 + 第一个 RAG 应用
- 环境:Windows + WSL2 + Docker Desktop(48G 内存 vs 云上 2G 的对比结论)
- 踩坑 1:ssh一直连不上
2G 服务器 SSH 反复僵死 现象:ssh root@IP 卡住无密码提示；后来 F12 复测显示 Connection timed out during banner exchange(门铃响没人应)。
 排查:ping 通 → 网络没问题；TCP 22 端口能连 → 机器开着、安全组没问题;Workbench 云助手执行报 ClientNotRunning → 连最轻量的后台服务都死了。三条证据指向：不是网络故障，是系统瘫痪。 
 原因:2G 内存跑 Dify 全家桶(官方最低要求 4G),内存耗尽导致内核依次饿死用户态进程；重启只是清空症状，几小时后必然复发。 
 解法：重启救急 + 明确两条正路(升配 4G 治本 / 停用非必要容器治标)。教训：部署前先查官方配置要求。
- 踩坑 2:现象 Dify 里保存模型配置必弹 Connection error;但 Key、网络、后端链路逐项检查都正常(用命令行模拟请求 0.1 秒就通)。 排查:F12 打开网络面板，重新点保存，看到请求真实响应是 400(参数不合法)——前端把它误显示成了 Connection error。
原因是：后端拒绝了表单——验证模型不在清单/Key 带杂质；前端又把 400 错显示成 Connection error);
解法是：不再迷信页面报错，按 F12 打开浏览器“网络”面板，重新点保存，直接看请求的真实响应——发现后端返回的其实是 400(参数不合法)，前端把它误显示成了"Connection error"。顺着 400 逐项核对表单，修正三处： ①验证模型名称改成插件模型清单里实际存在的 glm-5.3-flash(我原来填的 glm-4-flash 是老型号，清单里没有)； ②换用有标准端点额度的 API Key——我第一把“编程订阅”Key 只在 Anthropic 兼容端点有额度，Dify 走标准端点必然余额不足； ③重新纯净复制 Key,确保不带空格、换行和反引号。 三处修正后保存成功，模型接入完成。
- 成果:Chatflow 简历问答助手,glm-5.3-flash,提示词 3 条规则，Chatflow 简历问答助手(glm-5.3-flash 免费档 + 知识检索节点 + 3 条提示词规则)。验收：提问“简历主人会什么技能”，能按简历内容作答、答不出时明确声明“简历未提到”而非编造。
- 一句反思:部署前查配置要求  前端报错会骗人。
## 2026-09-21：2G 学习机 Dify 复活战（约 5 小时）

### 背景
2G 内存阿里云 ECS（Ubuntu 24.04）部署 Dify 全家桶（20 容器）。
部署完成后服务器反复"僵死"：网页 502 → 超时 → 最后 SSH 连密码
提示都不出现，只能靠控制台强制重启，但重启后过几分钟再次窒息。

### 排查过程（按时间线）
1. **误判阶段**：看到 502 就强制重启，前后重启多次，每次当时好几分钟、随后复发——陷入"502→重启→又502"死循环
2. **远程探活**：从自己电脑 curl 探测服务器 22/80 端口，发现22 端口能正常握手 → 机器活着，只是"慢+没响应"，不是死机
3. **浏览器定位**：F12 控制台看到失败的请求是 /console/api/system_features（502）→ 锁定 api 容器没上岗
4. **看日志**：docker logs 里 api 卡在 "Running migrations"  ——首次启动要跑几十步数据库迁移，2G 机器上要十几分钟，且每次重启都会把迁移进度打断回滚
5. **原因确认**：20 个容器内存需求超过物理内存，系统窒息；且容器 restart 策略导致每次重启全体自动复活，减负无法持久

### 最终解法（两步）
1. **永久减重**：docker update --restart=no 给 9 个非必需容器（sandbox×2、ssrf_proxy×2、agent_backend、plugin_daemon、 worker_beat、worker、weaviate）设置"重启不复活"，再 docker stop 它们——只保留 db/redis/api/web/nginx 核心五件套
2. **给迁移一条畅通的路**：docker restart docker-api-1，在宽裕 内存里重跑迁移，日志出现 "Database migration successful!" +"Listening at: http://0.0.0.0:5001"，浏览器打开
   http://8.159.129.135 → Dify 管理界面出现 ✅

### 途中还修掉的问题
- 镜像拉取僵死：公共加速站不可靠 → 换阿里云专属加速器（控制台-容器镜像服务-镜像加速器，走内网）
- 多行命令粘贴到 SSH/VNC 会断行错位（出现 dockertee.service这类粘连报错）→ 改用单行 printf 写配置文件
- SSH 卡在密码提示不出现：负载过高时 sshd 建会话极慢，多等 60 秒或走控制台 VNC 急救通道
- 低级失误三次：cd 后少空格、命令丢字母、占位符忘替换—— 报错信息其实都直接指出了原因

### 教训
1. 部署前先算资源账：20 容器的应用 ≠ 2G 机器能跑，最低配也要预留余量
2. 重启不是万能药：容器有 restart 策略时，重启=全体复活；要从机制（--restart=no）解决，不是反复重启
3. 迁移/初始化类操作被打断 = 进度回滚：看到 502 先查日志判断"在干活"还是"真挂了"，忍住不要动
4. 报障三要素：哪一步、敲了什么、屏幕原样显示什么——"还是不行"无法定位问题
5. 判断机器死活用端口探测，不要凭"看着没动静"下结论
