# Dify 部署问题排查指南

> 环境：2核2G 阿里云 ECS（上海）+ Ubuntu 24.04 + Docker 部署 Dify 1.17.1
> 每条按「现象 → 原因 → 解决 → 教训」记录，全部来自 2026-09-21 真实部署过程。

## 1. 全家桶挤爆内存，服务器窒息到 SSH 都连不上

- **现象**：`docker compose up -d` 拉起全部 20 个容器后，网页 502；随后 SSH 登录卡在输入密码前一动不动；终端命令全部无响应，但云控制台显示实例是"运行中"。
- **原因**：【你来填：20 个容器和 1.7G 内存是什么关系？swap 塞满后为什么连 SSH 都建不了会话？】
- **解决**：阿里云控制台强制重启 → 回来立刻 `docker stop` 砍掉 9 个非必需容器 → `docker update --restart=no` 让它们重启也不再复活 → 只保留核心五件套（db/redis/api/web/nginx）。
- **教训**：【你来填：一句话，关于"部署前该做什么"？】

## 2. 镜像拉取僵死：进度条几个小时纹丝不动

- **现象**：`docker compose up -d` 卡在 Pulling 某镜像，`/var/lib/docker` 始终 688K——一个字节都没下下来。
- **原因**：公共镜像加速站不稳定，连接挂起。
- **解决**：用 curl 逐个测加速站死活（401=活着）→ 换成阿里云控制台领取的**专属加速器**（走内网，快且稳）。
- **教训**：【你来填：遇到"卡住"时，先怀疑什么、再怀疑什么？】

## 3. 多行命令粘贴后变成乱码命令

- **现象**：粘贴一段 5 行的 `tee` 写配置命令后，报错 `Failed to restart dockertee.service`——"docker"和"tee"粘成了一个词；在 VNC 里粘贴则被拦腰截断。
- **原因**：多行文本在 SSH/VNC 粘贴时换行符丢失或错位，bash 收到的是几段粘在一起的碎片。
- **解决**：改用**单行命令**等价实现（如 `printf '...\n...' > 文件`），一行粘贴不可能错位。
- **教训**：【你来填：】（提示：和"操作环境差异"有关）

## 4. "Running migrations" 滚不完，期间反复 502

- **现象**：api 日志连续打印几十行 `Running migrations`，耗时以十分钟计；期间网页一直 502。曾三次忍不住强制重启，结果每次都把迁移进度打断重来，陷入"502→重启→又是502"的死循环。
- **原因**：首次启动要跑几十步数据库迁移，弱机上以十分钟计；重启会让迁移从上一个完成点重来。
- **解决**：`docker logs -f docker-api-1` 实时陪跑，直到出现 `Database migration successful!` 和 `Listening at:`，`docker compose ps` 显示 `(healthy)` 再操作浏览器。
- **教训**：【你来填：这一条里最反直觉的领悟是什么？】（提示：和"502 的含义"有关）

## 5. 命令打错的三种姿势

- **现象**：`cd/root/dify/docker`（少空格）报 No such file；`ocker logs`（少 d）报 command not found；在家目录跑 `docker compose ps` 报 no configuration file。
- **原因**：分别是空格丢失、字母丢失、以及——**compose 只认当前目录**，在错误的目录跑正确的命令一样失败。
- **解决**：逐字对照命令；bash 的报错往往直接给出正确拼写建议；进对目录再跑 compose。
- **教训**：【你来填：】

## 6. 截图转录 Secret 错了两个字符，签名失败

- **现象**：新 AccessKey 配置后 CLI 报 `IncompleteSignature`（签名不匹配）。
- **原因**：Secret 从截图人工转录，`0/O`、`l/I`、`g/q` 无法分辨——录入了多余的 `l`，还把 `q` 认成了 `g`。
- **解决**：让对方用**文本方式**重新提供（或官方 CSV 下载），逐字符修正后验证通过。
- **教训**：【你来填：】（提示：密钥的传递规范）

## 7. 浏览器 502 的三种含义

本次部署中 502 出现过三种完全不同的剧本：

1. 引擎没上岗：nginx 在，api 还在跑迁移——**等**就是解决方案；
2. 资源窒息：服务器连 SSH 都应不过来，网页自然瘫痪——**减重**是解决方案；
3. 时机问题：api 刚重启还在热身，页面恰好在这几秒请求——**重试**就是解决方案。

- **教训**：【你来填：同一个状态码背后可能是什么完全不同的事？定位时该看什么？】（提示：哪个工具/什么日志给出了定案）
